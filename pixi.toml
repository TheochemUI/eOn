[workspace]
authors = ["Rohit Goswami <rgoswami@ieee.org>"]
channels = ["conda-forge"]
name = "eOn"
# NOTE: not every platform has 100% support other than Linux
# e.g. LAMMPS and NWChem do not have builds for Windows
platforms = ["linux-64", "osx-64", "osx-arm64", "win-64"]
version = "2.13.0"

[tasks]
gen-ref = { cmd = "cd scripts/regression && snakemake -c4", description = "Generate SVN regression reference data (default: svn-Jul_01_2024)" }
gen-ref-clean = { cmd = "cd scripts/regression && snakemake clean", description = "Clean regression build artifacts" }
ensure_cbindgen = "command -v cbindgen > /dev/null || cargo install cbindgen"

[dependencies]
eigen = ">=3.4,<3.5"
# Pin python for torch
python = ">=3.11.5,<3.13"
# End manual pins
cmake = ">=4.0.3,<5"
highfive = ">=2.10.1,<3"
meson = ">=1.10.1,<2"
ninja = ">=1.12.1,<2"
pkgconf = ">=2.4.3,<3"
openblas = ">=0.3.29,<0.4"
pip = ">=25.1.1,<26"
pipx = ">=1.7.1,<2"
numpy = ">=2.3.0,<3"
compilers = ">=1.9.0,<2"
pkg-config = ">=0.29.2,<0.30"
fortran-compiler = ">=1.10.0,<2"
pyyaml = "*"
sccache = "*"
quill = ">=11.0.2,<12"
rust = ">=1.88"

[pypi-dependencies]
towncrier = ">=25.8.0, <26"
readcon = ">=0.7.0, <0.8"

[pypi-options]
# TODO(rg): only if the CPU version is needed
# set these on the command line instead
# export UV_EXTRA_INDEX_URL="https://download.pytorch.org/whl/cpu"
# export PIP_EXTRA_INDEX_URL="https://download.pytorch.org/whl/cpu"
extra-index-urls = ["https://download.pytorch.org/whl/cpu"]
index-strategy = "unsafe-best-match"

[environments]
metatomic = {features = ["metatomic"]}
dev-lite = {features = ["lint", "develop"]}
dev-xtb = {features = ["lint", "develop", "xtb"]}
dev = {features = ["lint", "develop", "nwchem", "metatomic", "xtb"]}
# ams is incompatible with metatomic
dev-mta = {features = ["lint", "develop", "metatomic"]}
lammps = {features = ["lammps"]}
ams = {features = ["ams"]}
nwchem = {features = ["nwchem"]}
docs = {features = ["docs"]}
docs-mta = {features = ["docs", "develop", "metatomic"]}
rel = {features = ["metatomic", "release"]}
serve = {features = ["serve"]}
dev-serve-mta = {features = ["serve", "metatomic", "develop"]}
# CI environments: minimal deps + specific potential for integration tests
ci-lammps = {features = ["develop", "lammps"]}
ci-xtb = {features = ["develop", "xtb"]}
ci-mta = {features = ["develop", "metatomic"]}

[feature.metatomic]
# vesin torch has a wheel issue with macos x86_64
# no torch wheels for 2.9 onwards for non arm macos
platforms = ["linux-64", "osx-arm64", "win-64"]

[feature.metatomic.dependencies]

[feature.metatomic.pypi-dependencies]
torch = ">=2.10, <2.11"
metatomic-torch = ">=0.1.9, <0.2"
metatensor-torch = ">=0.8.4, <0.9"
vesin-torch = ">=0.5.2, <0.6"
vesin = ">=0.5.2, <0.6"
metatrain = ">=2026.2.1, <2027"

[feature.ams.dependencies]
boost-cpp = "*"
abseil-cpp = "*"

[feature.lint.dependencies]
cppcheck = "*"
cpplint = "*"
clang-format = "*"
ruff = "*"
pre-commit = "*"

[feature.develop.pypi-dependencies]
ase = "*"

[feature.develop.dependencies]
ipython = ">=9.3.0,<10"

[feature.lammps]
platforms = ["linux-64", "osx-64", "osx-arm64"]

[feature.lammps.dependencies]
lammps = ">=2024.8.29,<2025"

[feature.nwchem]
platforms = ["linux-64", "osx-64", "osx-arm64"]
[feature.nwchem.dependencies]
nwchem = ">=7.2.3,<8"
i-pi = ">=3.1.5.1,<4"
pybind11 = ">=3.0.0,<4"

[feature.nwchem.pypi-dependencies]
psutil = ">=7.0.0, <8"

[feature.docs.dependencies]
sphinx = ">=7"
shibuya = "*"

[feature.docs.target.linux-64.dependencies]
ira = ">=2"

[feature.docs.pypi-dependencies]
meson-python = ">=0.17"
myst-nb = ">=1.1"
sphinx-design = "*"
sphinx-copybutton = "*"
sphinx-togglebutton = "*"
sphinx-favicon = "*"
sphinx-contributors = "*"
sphinx-sitemap = "*"
sphinxcontrib-bibtex = "*"
sphinxcontrib-spelling = "*"
autodoc-pydantic = "*"
sphinx-autodoc2 = "*"
rgpycrumbs = {version = ">=1.5", extras = ["analysis"]}
chemparseplot = {version = ">=1.5.4", extras = ["neb", "plot"]}
adjustText = "*"

[feature.docs.tasks.makedocs]
cmd = "sphinx-build docs/source docs/build/html"

[feature.docs.tasks.srvdocs]
depends-on = [
  { task = "makedocs", environment = "docs" },
]
cmd = """
cd docs/build/html && python -m http.server 8234
"""

[feature.release.dependencies]

[feature.xtb.dependencies]
xtb = "*"
tblite = "*"
tblite-python = "*"

[feature.serve]
platforms = ["linux-64", "osx-64", "osx-arm64", "win-64"]

[feature.serve.dependencies]
capnproto = ">=1.0,<2"

[feature.metatomic.tasks.mkeon]
args = ["buildtype"]
depends-on = [
  { task = "setupeon", environment = "eon", "args" = ["{{buildtype}}"] },
]
cmd = """
meson compile -C bbdir && \
meson install -C bbdir
"""

[feature.metatomic.tasks.setupeon]
# Remember that the mold stuff assumes that the user has a UNIX machine However
# dropping mold does nothing but leads to slightly longer build times so it
# isn't an issue
# For exact reproducibility -Dwith_ase_nwchem should be used but it shouldn't matter
# For debugging set
# --buildtype debug -Db_sanitize=address
# To prevent the subproject from leaking into the eOn history, it is easier to
# simply rsync in since it is ignored at that path anyway
#
# -Dwith_metatomic=True -Dpip_metatomic=True \
# -Dtorch_version=2.7 \
cmd = """
rsync -vaz ../gpr_optim subprojects && \
meson setup bbdir --reconfigure \
      --prefix=$CONDA_PREFIX \
      --libdir=lib --buildtype {{ buildtype }} \
      --native-file nativeFiles/mold.ini \
      --native-file nativeFiles/ccache_gnu.ini \
      -Dwith_xtb=False \
      -Dwith_metatomic=True \
      -Dpip_metatomic=True \
      -Dtorch_version=2.9 \
      -Dwith_tests=True \
      -Dwith_gprd=True
"""
args = ["buildtype"]
