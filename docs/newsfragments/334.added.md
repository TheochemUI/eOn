Added the `filin` option under `[ARTn]` to expose pARTn's input-file name. Empty (the default) leaves pARTn with its post-`artn_create` sentinel so no file is read; setting it to a path wires that file into pARTn setup and aborts early with a clear error if it is missing.
