`Matter::getForcesFree` / `getForcesFreeV` are const, and `setPositionsFree`
applies PBC wrapping like `setPositions` so free-atom optimizers stay consistent
for TIP4P/SPCE and other PBC pots (issue #171).
