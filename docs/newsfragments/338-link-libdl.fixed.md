Hoist `dl_dep = cppc.find_library('dl', required: false)` to file scope in `client/meson.build` so every loader-using target (LAMMPS, XTB, ARTn shim, IRA shim) links `-ldl` automatically. Pre-fix CI failed at link with `undefined reference to dlopen / dlsym / dlclose` for libxtbeon.so on Linux because only `potentials/LAMMPS/meson.build` had a local `dl_dep` and the new XtbLoader / ARTn / IRA paths inherited `_deps` without it.
