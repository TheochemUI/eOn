Bump default `warning_level` from 1 to 2 (`-Wall -Wextra`); the codebase compiles cleanly under both. Pin the inih r62 subproject to `warning_level=0` since it ships an `unused parameter 'user'` in `tests/unittest_alloc.c` that's vendored-upstream test code, not ours to fix. `warning_level=3` stays out of reach because it also passes `-fimplicit-none` to gfortran and breaks eonclient's vendored Fortran TUs that rely on implicit typing -- tracked under #117 for a Fortran-side cleanup.
